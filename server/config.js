const githubConfig ={
    client_id : "client_id",
    client_secret : "client_secret"
}
// this is for github authentication.

const mongodbConfig={
    username:"username",
    password:"password",
    host:"host",
    port:"port",
    dbname:"gpa"
}
//this is for mongodb authentication.


module.exports = {
    githubConfig,
    mongodbConfig
}
